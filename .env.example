# --------- General ---------
TIMEZONE=Asia/Ho_Chi_Minh

# FS advertised IPs (use auto for dev). For production set to your public/LAN IPs.
EXT_SIP_IP=auto
EXT_RTP_IP=auto

# --------- App (NestJS) ---------
APP_PORT=3000

# Demo tenants/users seed lives in src/data/seed-data.ts

# --------- Database (MySQL) ---------
DB_HOST=mysql
DB_PORT=3306
DB_USER=fsapp
DB_PASSWORD=fsapp
DB_ROOT_PASSWORD=rootpass
DB_NAME=freeswitch
DB_SYNC=true
DB_LOGGING=false
SEED_DEMO_DATA=true

# Shared secret for mod_xml_cdr webhook
CDR_SHARED_SECRET=changeme
