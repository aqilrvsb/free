# --------- General ---------
TIMEZONE=Asia/Ho_Chi_Minh

# FS advertised IPs (use auto for dev). For production set to your public/LAN IPs.
EXT_SIP_IP=192.168.110.16
EXT_RTP_IP=192.168.110.16

# --------- App (NestJS) ---------
APP_PORT=3000

# Demo tenants/users seed lives in src/data/seed-data.ts

NODE_ENV=production

# SignalWire FreeSWITCH repo access token (build arg)
FS_TOKEN=pat_5XdJ51UgrgFDLrjDkKhcb5F6

# --------- Database (MySQL) ---------
DB_HOST=mysql
DB_PORT=3306
DB_USER=fsapp
DB_PASSWORD=fsapp
DB_ROOT_PASSWORD=rootpass
DB_NAME=freeswitch
DB_SYNC=true
DB_LOGGING=false
SEED_DEMO_DATA=true

# Optional custom HTTP headers for mod_xml_cdr requests (e.g. "Authorization: Basic xxx")
CDR_HTTP_HEADERS=

# --------- FreeSWITCH Management ---------
FS_ESL_HOST=192.168.110.16
FS_ESL_PORT=8021
FS_ESL_PASSWORD=ClueCon
RECORDINGS_DIR=/recordings